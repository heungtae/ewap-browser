# S10 Browser 로컬 Page Profile 수락 종료 증거 — 2026-09-26

## 판정

`Completed` — 현행 Browser `schema_version: 1` compact ES256 JWS
Profile의 claim 검증과 로컬 replay high-water 범위. 기준 branch `master`,
기반 커밋 `f7d469b3` 위의 미커밋 변경을 검증했다. Platform release,
철회, `semanticId` binding의 완료 증거로 사용하지 않는다.

## 구현과 음성 검증

- `path_prefix=/case`가 `/case/123`을 받아들이고 `/cases`를 거부한다.
  `profile_version`은 안전한 양의 정수만 허용한다.
- `profile_replay_v1`에는 deployment/Profile 식별자의 해시, version,
  definition digest만 저장한다. 원시 Profile, page content, nonce,
  ref, action 값은 저장하지 않는다.
- 저장소를 새로 구성한 뒤에도 하위 version과 동일 version의 변경된
  정의를 거부한다. 손상된 기록, 저장 실패, 동시 갱신을 unit test로
  검증했다.
- HTTPS fixture의 실제 Side Panel에서 유효한 Profile version 1→2를
  수락하고 저장 내용을 확인했다. Service Worker 재시작 뒤 같은
  fixture가 돌려준 version 1은 `PROFILE_UNAVAILABLE`로 거부됐다.
  서명이 손상된 JWS도 거부됐다.
- 신규 저장소 연결로 실패하던 page context unit test를 수정했다.

## 실행 결과

| 명령·환경 | 결과 |
| --- | --- |
| `node_modules/.bin/tsc --noEmit` | 통과 |
| `node_modules/.bin/tsc -p tsconfig.build.json` | 통과 |
| `node scripts/build-extension.mjs` | unpacked extension 빌드 통과 |
| 관련 ESLint 및 Prettier 검사 | 통과 |
| `node scripts/check-module-boundaries.mjs` | TypeScript 228개 파일 통과 |
| `node_modules/.bin/vitest run extension/tests/unit` | 94개 파일, 326개 테스트 통과 |
| `CHROME_FOR_TESTING_BIN=/home/heungtae/.cache/ms-playwright/chromium-1217/chrome-linux64/chrome node --experimental-websocket scripts/chrome-s1-smoke.mjs` | Chrome for Testing 147.0.7727.15, 실제 Side Panel·Worker 재시작 통과; Resolver 5회, Provider 1회 |
| `git diff --check` | 통과 |

## 완료 범위 밖

Workspace `ewap/v1` 리소스와 Platform SignedRelease의 매핑,
현재 release 활성/철회 조회, `semanticId`→현재 projection runtime ref
해석은 구현·검증되지 않았다. 이 문서의 `Completed`는 그 기능이나
Enterprise 전체 Profile Runtime 완료를 뜻하지 않는다.
