# S0 종료 증거 — 2026-09-24

## 판정

`Completed` — [S0 계약](../sprints/s0-local-development-foundation.md)의
Linux Community 로컬 기반 범위. Windows 설치·대외 출시 판정은 포함하지 않는다.

## 재현 기준

- 소스: `d3bcf55226a5fbbeab96fb782eaf883ed3ccb3ea` (`master`의
  `git archive HEAD`를 `/tmp`의 격리된 디렉터리 두 곳에 전개)
- UTC 기록 시각: `2026-09-24T10:09:26Z`
- OS: Linux; Node `v22.23.2`; pnpm `9.15.4`
- Browser: Google Chrome for Testing `147.0.7727.15`
- 산출물: clean archive에서 `bump-extension-version.mjs`가
  `0.1.85 -> 0.1.86`으로 증가시킨 뒤 생성한 `dist-extension`
- 산출 manifest SHA-256:
  `6b258356898009da785d70418783918c2a1076be49f1ff99f5ca5e54b52d0f33`

## 실행 결과

| 검증 | 실행 | 결과 |
| --- | --- | --- |
| clean checkout 설치 | 공유 `node_modules` 없는 archive에서 pnpm 9 `install --offline --frozen-lockfile` | exit 0; lockfile 일치, 173 packages 재사용, 다운로드 0건 |
| clean source typecheck | Node 22 `node_modules/typescript/bin/tsc --noEmit` | exit 0 |
| clean source build | 독립 설치 디렉터리에서 pnpm 9 `build`; 다른 archive에서 version bump, `tsc -p tsconfig.build.json`, `scripts/build-extension.mjs` | 모두 exit 0; 두 산출물의 manifest/worker/panel hash 일치 |
| lint/format | pnpm 9 `lint` | exit 0 |
| package policy | `scripts/validate-package.mjs` | exit 0 |
| unit | 독립 설치 디렉터리에서 pnpm 9 `test:unit`; 다른 archive에서 Vitest 직접 실행 | 각각 79 files, 269 tests passed |
| fixture | Vitest `extension/tests/fixture` | 1 file, 1 test passed |
| source E2E | Vitest `extension/tests/e2e` | 1 file, 1 test passed |
| unpacked Chrome | Node 22 `scripts/chrome-extension-smoke.mjs` with `CHROME_FOR_TESTING_BIN` set to the browser above | exit 0; worker, Chat workspace/Side Panel document, Settings page loaded; sparse transcript recovery and diagnostic viewer passed |

Chrome smoke는 로컬 CDP와 새 임시 profile을 사용했다. Side Panel 문서를
extension URL로 열어 workspace를 확인했으며 native `chrome.sidePanel.open()`
사용자 gesture 전체는 S5 UI 검증 대상이다.
첫 오프라인 설치 시도는 sandbox가 esbuild postinstall 바이너리 실행을
`EPERM`으로 막았다. 같은 명령을 허용된 실행 환경에서 다시 수행해 통과했다.

## 제품 계정·패키지 경계

- Manifest에는 `identity` permission 또는 OAuth 선언이 없다.
- Permission snapshot: `storage`, `sidePanel`, `activeTab`, `tabs`,
  `scripting`, `debugger`, `offscreen`; host permission `https://*/*`;
  managed schema `managed-storage-schema.json`.
- `extension/src`, manifest 및 deployment에서 `chrome.storage.sync`,
  `chrome.identity`, `oauth2`, `subscription`, `Cloud Sync` 참조 검색 결과는
  0건이었다. 선택적 managed policy/identity 코드는 별도 Enterprise 범위다.
- Chrome 새 profile에서 worker, Chat workspace 및 Settings 로드까지
  별도 제품 로그인 단계가 필요하지 않았다.
- 현재 manifest의 permission, host permission 및 managed storage schema는
  `validate-package.mjs` 정책 검사와 일치한다.

이 증거는 S0 로컬 설치 기반을 검증한다. 실제 Provider, 운영 사이트의
Ask/Act 성공이나 Windows 동작을 증명하지 않는다.
